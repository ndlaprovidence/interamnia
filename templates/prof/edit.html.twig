{% extends 'base.html.twig' %}

{% block title %}Edit Prof{% endblock %}

{% block body %}
    <h1>Edit Prof</h1>

    {{ include('prof/_form.html.twig', {'button_label': 'Update'}) }}
    {{ include('prof/_delete_form.html.twig') }}

    <table class="table">
        <thead>
            <td>     
                <a title="Retour" href="{{ path('prof_index') }}"<button class="btn btn-primary"><i class="fa fa-arrow-left" aria-hidden="true"></i>
            </td>
        </thead>
    </table>
    
{% endblock %}
