{% extends 'base.html.twig' %}

{% block title %}Prof{% endblock %}

{% block body %}
    <h1>Prof</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ prof.id }}</td>
            </tr>
            <tr>
                <th>Nom</th>
                <td>{{ prof.nom }}</td>
            </tr>
            <tr>
                <th>Prenom</th>
                <td>{{ prof.prenom }}</td>
            </tr>
            <tr>
                <th>En_activite</th>
                <td>{{ prof.enActivite }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('prof_index') }}">back to list</a>

    <a href="{{ path('prof_edit', {'id': prof.id}) }}">edit</a>

    {{ include('prof/_delete_form.html.twig') }}
{% endblock %}
