{% extends 'base.html.twig' %}

{% block title %}Edit BTS{% endblock %}

{% block body %}
    <h1>Modifier le nom du BTS</h1>

    {{ include('bts/_form.html.twig', {'button_label': 'Mettre à jour'}) }}
    {{ include('bts/_delete_form.html.twig') }}
    
    <table class="table">
        <thead>
            <td>     
                <a title="Retour" href="{{ path('b_t_s_index') }}"<button class="btn btn-primary"><i class="fa fa-arrow-left" aria-hidden="true"></i>
            </td>
        </thead>
    </table>

    
{% endblock %}
