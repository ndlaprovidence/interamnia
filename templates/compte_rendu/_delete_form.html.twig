<form method="post" action="{{ path('compte_rendu_delete', {'id': compte_rendu.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_method" value="DELETE">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ compte_rendu.id) }}">
    <button class="btn">Supprimer</button>
</form>
