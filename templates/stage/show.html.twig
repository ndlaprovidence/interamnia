{% extends 'base.html.twig' %}

{% block title %}Stage{% endblock %}

{% block body %}
    <h1>Compte-rendu</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ stage.id }}</td>
            </tr>
            <tr>
                <th>Date_debut</th>
                <td>{{ stage.dateDebut }}</td>
            </tr>
            <tr>
                <th>Date_fin</th>
                <td>{{ stage.dateFin }}</td>
            </tr>
            <tr>
                <th>Theme</th>
                <td>{{ stage.theme }}</td>
            </tr>
            <tr>
                <th>Commentaire</th>
                <td>{{ stage.commentaire }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('stage_index') }}">Retour</a>

    <a href="{{ path('stage_edit', {'id': stage.id}) }}">Modifier</a>

    {{ include('stage/_delete_form.html.twig') }}
{% endblock %}
