{% extends 'base.html.twig' %}

{% block title %}Periode{% endblock %}

{% block body %}
    <h1>Periode</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ periode.id }}</td>
            </tr>
            <tr>
                <th>Date_debut</th>
                <td>{{ periode.dateDebut ? periode.dateDebut|date('Y-m-d H:i:s') : '' }}</td>
            </tr>
            <tr>
                <th>Date_fin</th>
                <td>{{ periode.dateFin ? periode.dateFin|date('Y-m-d H:i:s') : '' }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('periode_index') }}">back to list</a>

    <a href="{{ path('periode_edit', {'id': periode.id}) }}">edit</a>

    {{ include('periode/_delete_form.html.twig') }}
{% endblock %}
